/// <reference types="vite/client" />

/** The app version, injected at build time from package.json by
 *  electron.vite.config.ts so the About box cannot drift out of date. */
declare const __APP_VERSION__: string
